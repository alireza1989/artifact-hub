// mcp/ — MCP tool definitions, thin wrappers over core/ with rich descriptions
// (PLAN Phase 2, §4). Mounted at app/api/mcp via the stateless Streamable HTTP
// transport. Decision: use the stable @modelcontextprotocol/sdk (not the beta
// split @modelcontextprotocol/server) — verify against installed types in Phase 2.
export {};
